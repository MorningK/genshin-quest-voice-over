# FastAPI 运行时依赖
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.30

# 图像处理与 OCR
numpy>=2.5.1
opencv-python-headless>=4.10.0
onnxruntime>=1.28.0
rapidocr>=3.9.2

# 流式 TTS（Edge TTS）
edge-tts>=6.1.0

# 项目自身（可编辑安装）
# 改造为 src-layout 后，genshin_voice_over 位于 src/ 下，不再随 CWD 自动可见。
# Vercel 若走 pip install -r requirements.txt 的路径，缺此行会 ModuleNotFoundError；
# Vercel 优先读取 pyproject.toml 时本行无副作用。
-e .
